# Benchmarks

This folder stores exported training metrics for transparent model comparison.

- `all_model_results.csv`: consolidated registry
- `canopy/metrics.csv`: canopy model metrics
- `landcover/metrics.csv`: land-cover model metrics

Metrics are generated by `scripts/train_model.py` and post-processed into this folder.
